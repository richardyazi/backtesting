# 股票数据缓存脚本依赖库
# 在聚宽环境中，以下库通常已预装

# 数据科学核心库
pandas>=1.3.0
numpy>=1.20.0

# 数据存储格式
pyarrow>=5.0.0  # Parquet格式支持

# 日志和配置
pyyaml>=5.4.0   # YAML配置支持

# 开发工具
ipython>=7.0.0  # Jupyter Notebook支持
jupyter>=1.0.0

# 数据可视化（可选）
matplotlib>=3.3.0
seaborn>=0.11.0

# 注意：聚宽API (jqdatasdk) 在聚宽环境中已预装，无需额外安装